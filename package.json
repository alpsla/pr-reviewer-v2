{
  "name": "pr-reviewer",
  "private": true,
  "scripts": {
    "build": "pnpm --filter './packages/**' build",
    "clean": "pnpm -r clean",
    "dev": "pnpm --filter web dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
